import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  input: {
    color: '#fff',
    fontSize: 28,
    fontWeight: '600',
    lineHeight: 35,
    flexShrink: 0,
    padding: 0,
    margin: 0
  }
})
